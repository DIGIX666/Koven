{
  "name": "@koven/audit",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc -b tsconfig.build.json",
    "lint": "eslint . --max-warnings 0",
    "test": "vitest run --root ../.. --project @koven/audit --passWithNoTests"
  }
}
