{
  "name": "@koven/schemas",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "dependencies": {
    "zod": "3.25.76",
    "@koven/domain": "workspace:*",
    "@koven/audit": "workspace:*"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --root ../.. --project @koven/schemas",
    "build": "tsc -b tsconfig.build.json",
    "lint": "eslint . --max-warnings 0"
  }
}
